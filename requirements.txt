# RobustIShip Agent - CPU Only

# Gemma model inference
torch>=2.0.0
transformers>=4.30.0

# Environment
python-dotenv>=1.0.0

# Optional but recommended
accelerate>=0.20.0    # Faster model loading
bitsandbytes>=0.41.0  # 4-bit/8-bit quantization (saves RAM)
psutil>=5.9.0         # System memory info (SystemMemory uses this)
